# HTB Machines Checklist

A comprehensive checklist of HackTheBox machines, categorized by Operating System and Difficulty. This project also includes a professional writeup template for documenting exploitation processes.

## Project Structure
- `Machines.md`: The main checklist of HTB machines.
- `WriteupTemplate.md`: A structured template for documenting machine exploitation.
- `.gitignore`: Configured to ignore temporary files and system-specific metadata.

## Generation
The `Machines.md` file was generated using the following command:
```bash
curl -s -L -A "Mozilla/5.0" https://www.hackthebox.com/machines | grep -oP '(?<=<span class="color-white">).*?(?=</span>)' | sort -u
```

## Maintenance & Updates

### Git Workflow
To push updates to GitHub, use the following commands:

1. **Initial Setup (if not already done):**
   ```bash
   git remote add origin <YOUR_GITHUB_REPO_URL>
   git branch -M main
   ```

2. **Pushing Changes:**
   ```bash
   git add .
   git commit -m "Update machines list and template"
   git push -u origin main
   ```

3. **Subsequent Pushes:**
   ```bash
   git push
   ```

## Author
Generated by RF2i
